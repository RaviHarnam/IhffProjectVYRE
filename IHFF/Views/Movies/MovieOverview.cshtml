@using IHFF.Models;


@{
    ViewBag.Title = "MovieOverview";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@Styles.Render("~/Content/overview.css")
@Scripts.Render("~/bundles/modernizr")
<script src="~/Scripts/jquery-1.8.2.js"></script>
<script src="~/Scripts/jquery-ui-1.8.24.min.js"></script>

@model IEnumerable<Movie>

<h2>Movie Overview</h2>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>

    @{int movieNr = 0; }
    @*<select id="filmlocationinput" name="locationid">
            @foreach (FilmLocation locatie in Model.Film.FilmLocation)
                {
                <option value="@locatie.Id">@locatie.Location: @locatie.Begintime - @locatie.Endtime</option>
            }
        </select>*@
    <table>
        @Html.Hidden("movieId", 0)
        @for (int i = 0; i < Model.Count(); i++)
        {
            <tr>
                @for (int j = 0; j < 3; j++)
                {
                    if (movieNr < Model.Count())
                    {
                        if (Model.ElementAt(movieNr).ItemAfbeelding != null)
                        {
                            <td>

                                <div class="col-lg-3 col-md-4 col-sm-6 col-lg-12">
                                    <div class="hovereffect">
                                        @*<p class="movie_index_item_title">@Model.ElementAt(movieNr).Titel</p>*@
                                        <img class="img-responsive" src=@Model.ElementAt(movieNr).ItemAfbeelding.Link alt=@Model.ElementAt(movieNr).Titel title="@Model.ElementAt(movieNr).Titel">
                                        <div class="overlay">


                                            <h2>@Model.ElementAt(movieNr).Titel</h2>


                                            <!-- Trigger/Open The Modal -->
                                            <!-- The Modal -->

                                            <button id="@movieNr" class="movie_index_item_description_button">Voeg toe aan Cart</button>
                                            <div class="modal">
                                                <article class="modal-content">
                                                    <article class="modal-header">
                                                        <button onclick="$(this).parent('div').style.display = 'none'" class="close">&times;</button>

                                                        <h2>Modal Header</h2>
                                                    </article>
                                                    <div class="modal-body">
                                                        @Html.Partial("_MovieOverview", @Model.ElementAt(movieNr))
                                                        @*@using (Html.BeginForm("AddToCart", "Cart"))
                                                            {
                                                                <div class="pull-right">@Html.Hidden("voorstellingid", )
                                                             @Html.Hidden("returnUrl", Request.Url.PathAndQuery)<input type="submit" class="movie_index_item_description_button" value="Add  tocart" /></div>
                                                            }*@
                                                       
                                                    </div>
                                                    <div class="modal-footer">
                                                        <h3>Do you have dinner plans yet? <!--Html.ActionLink("", "", "", null, new {Class = "" })--></h3>
                                                    </div>
                                                </article>
                                            </div>
                                            @*<script src="@Url.Content("~/Scripts/jquery-ui-1.8.11.js")" type="text/javascript"></script>
                                                <script type="text/javascript">
                                                    function toonpopup() {
                                                        $("#inputform").slideDown("slow");

                                                        modal.style.display = "block";
                                                    }
                                                </script>*@
                                            <a href="SpaceOdysses.html" class="movie_index_item_description_button2">Wishlist</a>  <!--a ref naar actionlink-->
                                        </div>
                                    </div>

                                </div>
                                <p class="movie_index_item_title">@Html.ActionLink(Model.ElementAt(movieNr).Titel, "MovieDetailPage", "Movies", new { movie_id = Model.ElementAt(movieNr).ItemID }, null) </p>
                                @*<div id="inputform">
                                        @using (Html.BeginForm())
                                        {

                                            <input type="number" id="aantalinvoeren" name="aantal" />
                                            <input type="hidden" value="@movieNr" name="filmid" />
                                            <button id="submitknop" onclick="form.submit();"> click me</button>
                                        }
                                    </div>*@
                            </td>


                            { movieNr++; }


                        }
                    }
                }



            </tr>
        }
    </table>

    <br />
    @Scripts.Render("~/bundles/jquery")
    @*<script type="text/javascript">
            function ShowTitle()
            {
                var x = document.getElementById("ItemId")
                var xid = document.getElementById("ItemId")

            }


        </script>*@

    <script>
        $(document).ready(function () {
            // Get the modal
            //var modal = document.getElementsByClassName("modal")[0];

            // Get the button that opens the modal
            //var btn = document.getElementsByClassName("movie_index_item_description_button");

            // Get the <span> element that closes the modal
            //var span = document.getElementsByClassName("close")[0];

            // When the user clicks the button, open the modal
            //btn.onclick = function myFunction(event) {

            //    modal.style.display = "block";
            //    event.target.btn;
            //}

            //When the user clicks on <span> (x), close the modal
            //span.onclick = function () {
            //    modal.style.display = "none";
            //}

            // When the user clicks anywhere outside of the modal, close it
            //window.onclick = function (event) {
            //    if (event.target == modal) {
            //        //modal.style.display = "none";
            //}
            //}

            // When the user clicks anywhere outside of the modal, close it
            $('.close').click(function () {
                // If the clicked object is a modal.
                $('.modal').css('display', 'none');
            });
            //function closePopup() {
            //    var modal = document.getElementsByClassName("modal")[0];
            //    modal.style.display = "none";
            //}

            //$('.close').click(function () {
            //    $(this).prev().prev().prev().css('display', 'none');
            //});

            $('.movie_index_item_description_button').click(function () {
                $(this).next()[0].style.display = "inline";
                var title = $(this).prev('h2').html();
                $('.modal-header').find('h2').html(title);
            });
        });
    </script>
</body>
</html>
